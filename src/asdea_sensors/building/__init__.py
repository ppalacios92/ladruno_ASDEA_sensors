"""Building layer: structural characterization from the sensor data.

Uses the signal machinery plus the sensor geometry (config.SENSOR_GEOMETRY) to
describe the building as a whole: transfer functions, coherence, mode shapes,
damping, torsion, interstory drift and base rocking. Methods work across all
sensors, not just a pair.
"""
